# Что проверить в Railway для автодеплоя

## ✅ Что уже настроено правильно (из скриншотов):

1. **Custom Start Command:** `npm start` ✅
   - Правильно настроено в `railway.json`
   - Railway использует эту команду для запуска

2. **Restart Policy:** On Failure, 10 attempts ✅
   - Правильно настроено в `railway.json`
   - Контейнер будет перезапускаться при ошибках

3. **Resource Limits:** Настроены ✅
   - CPU и Memory выделены
   - Этого достаточно для работы

## ⚠️ Что нужно проверить (не видно на скриншотах):

### 1. Source (Источник) - САМОЕ ВАЖНОЕ!

**Где найти:** Settings → Service → **Source**

Проверьте:
- ✅ **Repository:** `Nalivator3000/UA-IP-parcer`
- ✅ **Branch:** `main`
- ✅ **Status:** Connected/Linked
- ✅ **Auto Deploy:** ВКЛЮЧЕН (переключатель активен)

**Если Auto Deploy выключен:**
- Включите переключатель
- Сохраните изменения

### 2. Root Directory

**Где найти:** Settings → Service → **Root Directory**

**ВАЖНО:** Должно быть **ПУСТЫМ** (не `csv_export_app`)

Проект находится в корне репозитория, поэтому Root Directory не нужен.

### 3. Healthcheck Path (добавлено в railway.json)

**Где найти:** Settings → Service → **Deploy** → Healthcheck Path

Теперь в `railway.json` добавлен:
```json
"healthcheckPath": "/api/health"
```

Railway будет проверять этот endpoint перед завершением деплоя.

**Проверьте в Railway:**
- Должен появиться Healthcheck Path: `/api/health`
- Если не появился автоматически - добавьте вручную

## 🔍 Что проверить дальше:

1. **Deployments** (Railway Dashboard → Deployments):
   - Когда был последний деплой?
   - Какой статус? (Success / Failed / Building)
   - Есть ли ошибки в логах?

2. **Webhook на GitHub:**
   - GitHub → Settings → Webhooks
   - Должен быть webhook от Railway
   - Проверьте последние доставки

3. **Переменные окружения:**
   - Railway → Settings → Variables
   - Должна быть `DATABASE_URL`

## 🚀 Быстрое решение:

Если автодеплой не работает:

1. **Проверьте Source:**
   - Settings → Service → Source
   - Убедитесь, что Auto Deploy ВКЛЮЧЕН
   - Убедитесь, что ветка `main`

2. **Попробуйте ручной деплой:**
   - Deployments → Deploy/Redeploy
   - Это проверит, работает ли деплой вообще

3. **Проверьте логи:**
   - Deployments → выберите последний деплой → Logs
   - Ищите ошибки

## 📝 Изменения в railway.json:

Добавлен `healthcheckPath` для проверки работоспособности после деплоя:
- Railway будет проверять `/api/health` перед завершением деплоя
- Если healthcheck не проходит - деплой будет считаться неудачным

