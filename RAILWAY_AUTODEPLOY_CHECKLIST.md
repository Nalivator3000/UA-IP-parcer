# Чеклист проверки автодеплоя Railway

## ✅ Проверка 1: Структура репозитория

**Репозиторий:** `Nalivator3000/UA-IP-parcer`
**Ветка:** `main`

Файлы должны быть в **корне** репозитория:
- ✅ `package.json` - есть
- ✅ `server.js` - есть  
- ✅ `railway.json` - есть
- ✅ `public/index.html` - есть
- ✅ `Procfile` - есть (опционально)

**Статус:** ✅ Структура правильная

---

## ✅ Проверка 2: Последние коммиты

Последние коммиты в `main`:
- `ebaa8a7` - Add local testing script and improve error handling
- `8af6e2e` - Add timeout for columns check query
- `da5d236` - Add timeouts to prevent hanging requests
- `3bb4be3` - Remove categories section and fix event types selection

**Статус:** ✅ Коммиты есть и запушены

---

## ⚠️ Проверка 3: Настройки Railway (нужно проверить вручную)

### 3.1 Подключение репозитория

1. Откройте [Railway Dashboard](https://railway.app/dashboard)
2. Найдите проект `UA-IP-parcer`
3. Перейдите в **Settings** → **Service** → **Source**
4. Проверьте:
   - **Repository:** должно быть `Nalivator3000/UA-IP-parcer`
   - **Branch:** должно быть `main`
   - **Status:** должно быть "Connected" или "Linked"

**Если репозиторий не подключен:**
- Нажмите **"Connect Repo"** или **"Change Source"**
- Выберите `Nalivator3000/UA-IP-parcer`
- Выберите ветку `main`

### 3.2 Автодеплой

1. В **Settings** → **Service** → **Deploy**
2. Проверьте:
   - **Auto Deploy:** должен быть **ВКЛЮЧЕН** (переключатель активен)
   - **Branch:** должно быть `main`
   - **Watch Paths:** можно оставить пустым (деплоит при любых изменениях)

**Если Auto Deploy выключен:**
- Включите переключатель
- Сохраните изменения

### 3.3 Root Directory

**ВАЖНО:** Проект находится в **корне** репозитория!

1. В **Settings** → **Service**
2. Найдите **"Root Directory"** или **"Working Directory"**
3. Должно быть **ПУСТЫМ** (не `csv_export_app`)

**Если указан Root Directory:**
- Очистите поле (оставьте пустым)
- Сохраните изменения

---

## ⚠️ Проверка 4: Webhook на GitHub

1. Откройте репозиторий на GitHub: https://github.com/Nalivator3000/UA-IP-parcer
2. Перейдите в **Settings** → **Webhooks**
3. Должен быть webhook от Railway (обычно URL содержит `railway.app`)

**Если webhook отсутствует:**
- Переподключите репозиторий в Railway
- Или создайте webhook вручную:
  - **Payload URL:** `https://api.railway.app/v1/webhooks/github` (или другой URL от Railway)
  - **Content type:** `application/json`
  - **Events:** `Just the push event`
  - **Active:** ✅

**Если webhook есть, но не работает:**
- Откройте **Recent Deliveries**
- Проверьте последние доставки
- Если есть ошибки - переподключите репозиторий

---

## ⚠️ Проверка 5: Переменные окружения

В Railway **Settings** → **Variables** должны быть:

- ✅ `DATABASE_URL` - строка подключения к PostgreSQL
- ⚠️ `PORT` - Railway установит автоматически (не обязательно)
- ⚠️ `NODE_ENV` - опционально (можно установить `production`)

**Проверьте, что `DATABASE_URL` правильный!**

---

## ⚠️ Проверка 6: Последний деплой

1. В Railway Dashboard → **Deployments**
2. Проверьте последний деплой:
   - Когда был последний деплой?
   - Какой статус? (Success / Failed / Building)
   - Есть ли ошибки в логах?

**Если последний деплой старый:**
- Попробуйте сделать ручной **Redeploy**
- Проверьте логи на наличие ошибок

**Если деплой падает с ошибкой:**
- Проверьте логи сборки
- Убедитесь, что `package.json` правильный
- Проверьте, что все зависимости установлены

---

## 🔧 Быстрое решение

Если ничего не помогает, попробуйте:

1. **Переподключить репозиторий:**
   - Railway Dashboard → Settings → Service → Source
   - Нажмите **"Disconnect"**
   - Затем **"Connect Repo"** → выберите `UA-IP-parcer` → `main`

2. **Ручной деплой:**
   - Railway Dashboard → Deployments
   - Нажмите **"Deploy"** или **"Redeploy"**
   - Это проверит, работает ли деплой вообще

3. **Проверить логи:**
   - Railway Dashboard → Logs
   - Посмотрите, есть ли ошибки при запуске

---

## 📝 Что проверить дальше

После проверки всех пунктов выше, если автодеплой все еще не работает:

1. Проверьте, что вы пушите в правильную ветку (`main`)
2. Убедитесь, что коммиты действительно попадают на GitHub
3. Проверьте права доступа Railway к репозиторию в GitHub Settings → Applications

---

## 🆘 Если нужна помощь

Соберите следующую информацию:
1. Скриншот Settings → Service → Source в Railway
2. Скриншот Settings → Service → Deploy в Railway
3. Скриншот Webhooks в GitHub
4. Логи последнего деплоя из Railway

